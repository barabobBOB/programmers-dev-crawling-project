import React from 'react';
import ReactDOM from 'react-dom';
import CoinList from './App';

ReactDOM.render(
  <React.StrictMode>
    <CoinList />
  </React.StrictMode>,
  document.getElementById('root')
);
